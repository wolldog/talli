import React from "react";
import { Typography } from "antd";

const Login = () => {
  return (
    <div className="login">
      <Typography.Text>Login Form - You will only see this if you are not logged in.</Typography.Text>
    </div>
  );
};

export default Login;
